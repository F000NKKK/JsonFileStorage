<Window x:Class="TestSoft.FileStorageWPFManager.ResponseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestSoft.FileStorageWPFManager"
        mc:Ignorable="d"
        Title="Response Window" Height="450" Width="800">
    <Grid>
        <!-- ScrollViewer for both vertical and horizontal scrolling -->
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <!-- TextBox that occupies all available space in the Grid -->
            <TextBox x:Name="ResponseTextBox" 
                     IsReadOnly="True" 
                     AcceptsReturn="True" 
                     VerticalScrollBarVisibility="Auto" 
                     HorizontalScrollBarVisibility="Auto" 
                     TextWrapping="Wrap" 
                     Margin="0" 
                     VerticalAlignment="Stretch" 
                     HorizontalAlignment="Stretch" />
        </ScrollViewer>
    </Grid>
</Window>

